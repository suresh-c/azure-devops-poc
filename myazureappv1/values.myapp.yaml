replicaCount: 2

image:
  repository: sureshintrospect1b.azurecr.io/myazureappv1
  tag: "latest"     # overridden by pipeline
  pullPolicy: IfNotPresent
imagePullSecrets:
  - name: acr-secret

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

ingress:
  enabled: false

resources: {}
